# Quant API — Python dependencies
# Install: pip install -r requirements.txt
 
# Web framework
fastapi>=0.111.0,<0.120.0
 
# ASGI server
uvicorn[standard]>=0.29.0,<0.35.0
 
# Async HTTP client
httpx>=0.27.0,<0.29.0
 
# Dataframe / indicator engine
polars>=0.20.0,<1.0.0
 
# In-memory TTL + LRU cache
cachetools>=5.3.0,<6.0.0
 
# Data validation (FastAPI response models)
pydantic>=2.6.0,<3.0.0
 
